# This is the path where the tools will read "binary.so"
# and write "symbols.txt", "typeinfos.txt"
ABS_PATH = "C:\\SomePath\\"

# This is the folder where vtable files are saved
VTABLE_PATH = ABS_PATH + "vtables\\"

# This is the folder where headers are saved
HEADERS_PATH = ABS_PATH + "headers\\"

# Set to false if you're targetting 64-bit binaries
THUMB_MODE = True